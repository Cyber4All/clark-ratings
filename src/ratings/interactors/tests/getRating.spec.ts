describe('When getRating is called', () => {
    describe('and an existing rating is requested', () => {
        it('should return a rating', () => {
            expect(false).toBeTruthy();
        });
    });
    describe('and the requested rating is not found', () => {
        it('should throw a not found error', () => {
            expect(false).toBeTruthy();
        });
    });
});
